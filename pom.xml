<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.lytrax</groupId>
    <artifactId>accessconverter</artifactId>
    <version>3.1.0</version>

    <name>AccessConverter</name>
    <description>Converts Microsoft Access databases (.mdb, .accdb) to MySQL/MariaDB dumps, SQLite and JSON</description>
    <url>https://github.com/clytras/AccessConverter</url>

    <licenses>
        <license>
            <name>Apache-2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0</url>
        </license>
    </licenses>

    <scm>
        <url>https://github.com/clytras/AccessConverter</url>
        <connection>scm:git:https://github.com/clytras/AccessConverter.git</connection>
    </scm>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <!-- Reproducible builds: fixed entry timestamps in every jar -->
        <project.build.outputTimestamp>2026-09-22T00:00:00Z</project.build.outputTimestamp>

        <main.class>io.lytrax.accessconverter.cli.Main</main.class>

        <!-- Runtime -->
        <jackcess.version>5.0.1</jackcess.version>
        <jackcess-encrypt.version>5.0.0</jackcess-encrypt.version>
        <bouncycastle.version>1.86</bouncycastle.version>
        <sqlite-jdbc.version>3.53.4.0</sqlite-jdbc.version>
        <jackson.version>3.2.3</jackson.version>
        <picocli.version>4.7.7</picocli.version>

        <!-- Test. JUnit stays on 5.14: jqwik 1.10 is built against JUnit Platform 1.14 -->
        <junit.version>5.14.4</junit.version>
        <assertj.version>3.27.7</assertj.version>
        <jqwik.version>1.10.1</jqwik.version>
        <testcontainers.version>2.0.5</testcontainers.version>
        <mysql-connector.version>26.7.0</mysql-connector.version>
        <mariadb-client.version>3.5.10</mariadb-client.version>
        <json-schema-validator.version>3.0.7</json-schema-validator.version>

        <!-- Plugins -->
        <maven-clean-plugin.version>3.5.0</maven-clean-plugin.version>
        <maven-compiler-plugin.version>3.16.0</maven-compiler-plugin.version>
        <maven-deploy-plugin.version>3.2.0</maven-deploy-plugin.version>
        <maven-enforcer-plugin.version>3.6.3</maven-enforcer-plugin.version>
        <extra-enforcer-rules.version>1.12.1</extra-enforcer-rules.version>
        <maven-failsafe-plugin.version>3.6.0</maven-failsafe-plugin.version>
        <maven-install-plugin.version>3.2.0</maven-install-plugin.version>
        <maven-jar-plugin.version>3.5.1</maven-jar-plugin.version>
        <maven-resources-plugin.version>3.5.0</maven-resources-plugin.version>
        <maven-shade-plugin.version>3.6.2</maven-shade-plugin.version>
        <maven-site-plugin.version>3.22.0</maven-site-plugin.version>
        <maven-surefire-plugin.version>3.6.0</maven-surefire-plugin.version>
        <maven-wrapper-plugin.version>3.3.4</maven-wrapper-plugin.version>
        <jacoco-maven-plugin.version>0.8.15</jacoco-maven-plugin.version>
        <spotless-maven-plugin.version>3.10.2</spotless-maven-plugin.version>
        <palantir-java-format.version>2.98.0</palantir-java-format.version>

        <!-- Test runs -->
        <argLine/>
        <fixtures.dir>${project.build.directory}/fixtures</fixtures.dir>
        <test.excludedGroups>local-samples,perf</test.excludedGroups>
        <skipITs>true</skipITs>
        <it.db.images>mysql:8.0,mysql:8.4,mariadb:10.11,mariadb:11.4,mariadb:11.8</it.db.images>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- jackcess-encrypt 5.0.0 declares jackcess 5.0.0 -->
            <dependency>
                <groupId>com.healthmarketscience.jackcess</groupId>
                <artifactId>jackcess</artifactId>
                <version>${jackcess.version}</version>
            </dependency>
            <dependency>
                <groupId>org.junit</groupId>
                <artifactId>junit-bom</artifactId>
                <version>${junit.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers-bom</artifactId>
                <version>${testcontainers.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>com.healthmarketscience.jackcess</groupId>
            <artifactId>jackcess</artifactId>
        </dependency>
        <!-- Encoded (Access 97, Jet4) and encrypted (Office 2007+) databases -->
        <dependency>
            <groupId>com.healthmarketscience.jackcess</groupId>
            <artifactId>jackcess-encrypt</artifactId>
            <version>${jackcess-encrypt.version}</version>
        </dependency>
        <dependency>
            <groupId>org.bouncycastle</groupId>
            <artifactId>bcprov-jdk18on</artifactId>
            <version>${bouncycastle.version}</version>
        </dependency>
        <dependency>
            <groupId>org.xerial</groupId>
            <artifactId>sqlite-jdbc</artifactId>
            <version>${sqlite-jdbc.version}</version>
        </dependency>
        <dependency>
            <groupId>tools.jackson.core</groupId>
            <artifactId>jackson-core</artifactId>
            <version>${jackson.version}</version>
        </dependency>
        <dependency>
            <groupId>info.picocli</groupId>
            <artifactId>picocli</artifactId>
            <version>${picocli.version}</version>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>${assertj.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>net.jqwik</groupId>
            <artifactId>jqwik</artifactId>
            <version>${jqwik.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-mysql</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-mariadb</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <version>${mysql-connector.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mariadb.jdbc</groupId>
            <artifactId>mariadb-java-client</artifactId>
            <version>${mariadb-client.version}</version>
            <scope>test</scope>
        </dependency>
        <!-- Validates JSON exports against the published schema (07) -->
        <dependency>
            <groupId>com.networknt</groupId>
            <artifactId>json-schema-validator</artifactId>
            <version>${json-schema-validator.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <artifactId>maven-clean-plugin</artifactId>
                    <version>${maven-clean-plugin.version}</version>
                </plugin>
                <plugin>
                    <artifactId>maven-resources-plugin</artifactId>
                    <version>${maven-resources-plugin.version}</version>
                </plugin>
                <plugin>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven-compiler-plugin.version}</version>
                </plugin>
                <plugin>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${maven-surefire-plugin.version}</version>
                </plugin>
                <plugin>
                    <artifactId>maven-failsafe-plugin</artifactId>
                    <version>${maven-failsafe-plugin.version}</version>
                </plugin>
                <plugin>
                    <artifactId>maven-jar-plugin</artifactId>
                    <version>${maven-jar-plugin.version}</version>
                </plugin>
                <plugin>
                    <artifactId>maven-shade-plugin</artifactId>
                    <version>${maven-shade-plugin.version}</version>
                </plugin>
                <plugin>
                    <artifactId>maven-install-plugin</artifactId>
                    <version>${maven-install-plugin.version}</version>
                </plugin>
                <plugin>
                    <artifactId>maven-deploy-plugin</artifactId>
                    <version>${maven-deploy-plugin.version}</version>
                </plugin>
                <plugin>
                    <artifactId>maven-site-plugin</artifactId>
                    <version>${maven-site-plugin.version}</version>
                </plugin>
                <plugin>
                    <artifactId>maven-wrapper-plugin</artifactId>
                    <version>${maven-wrapper-plugin.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>
            <plugin>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>${maven-enforcer-plugin.version}</version>
                <dependencies>
                    <dependency>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>extra-enforcer-rules</artifactId>
                        <version>${extra-enforcer-rules.version}</version>
                    </dependency>
                </dependencies>
                <executions>
                    <execution>
                        <id>enforce</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <requireJavaVersion>
                                    <version>[21,)</version>
                                </requireJavaVersion>
                                <requireMavenVersion>
                                    <version>[3.9,)</version>
                                </requireMavenVersion>
                                <requirePluginVersions/>
                                <banDuplicatePomDependencyVersions/>
                                <dependencyConvergence/>
                                <!-- Nearest-wins must never pick a version older than a dependency requires. Test scope
                                     is included (excluded by default): an old commons-io once broke Testcontainers. -->
                                <requireUpperBoundDeps>
                                    <excludedScopes>
                                        <excludedScope>none</excludedScope>
                                    </excludedScopes>
                                </requireUpperBoundDeps>
                                <enforceBytecodeVersion>
                                    <maxJdkVersion>21</maxJdkVersion>
                                    <ignoredScopes>
                                        <ignoredScope>test</ignoredScope>
                                    </ignoredScopes>
                                </enforceBytecodeVersion>
                                <!-- 02: the runtime tree is exactly Jackcess (with jackcess-encrypt and BouncyCastle),
                                     sqlite-jdbc, jackson-core and picocli -->
                                <bannedDependencies>
                                    <excludes>
                                        <exclude>*:*:*:*:compile</exclude>
                                        <exclude>*:*:*:*:runtime</exclude>
                                        <exclude>junit:junit</exclude>
                                    </excludes>
                                    <includes>
                                        <include>com.healthmarketscience.jackcess:jackcess</include>
                                        <include>com.healthmarketscience.jackcess:jackcess-encrypt</include>
                                        <include>org.bouncycastle:bcprov-jdk18on</include>
                                        <include>org.xerial:sqlite-jdbc</include>
                                        <include>tools.jackson.core:jackson-core</include>
                                        <include>info.picocli:picocli</include>
                                    </includes>
                                </bannedDependencies>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <compilerArgs>
                        <arg>-Xlint:all</arg>
                    </compilerArgs>
                </configuration>
            </plugin>

            <plugin>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <excludedGroups>${test.excludedGroups}</excludedGroups>
                    <trimStackTrace>false</trimStackTrace>
                    <systemPropertyVariables>
                        <fixtures.dir>${fixtures.dir}</fixtures.dir>
                        <samples.root>${project.basedir}</samples.root>
                    </systemPropertyVariables>
                </configuration>
                <executions>
                    <!-- Every unit test runs twice, under two locales and time zones that differ from the
                         canonical output (Locale.ROOT, no zone): el-GR has a decimal comma. -->
                    <execution>
                        <id>default-test</id>
                        <configuration>
                            <argLine>@{argLine} -Duser.language=el -Duser.country=GR -Duser.timezone=Europe/Athens</argLine>
                            <systemPropertyVariables>
                                <expected.locale>el-GR</expected.locale>
                                <expected.timezone>Europe/Athens</expected.timezone>
                            </systemPropertyVariables>
                        </configuration>
                    </execution>
                    <execution>
                        <id>en-US</id>
                        <goals>
                            <goal>test</goal>
                        </goals>
                        <configuration>
                            <argLine>@{argLine} -Duser.language=en -Duser.country=US -Duser.timezone=America/Los_Angeles</argLine>
                            <reportsDirectory>${project.build.directory}/surefire-reports-en-US</reportsDirectory>
                            <systemPropertyVariables>
                                <expected.locale>en-US</expected.locale>
                                <expected.timezone>America/Los_Angeles</expected.timezone>
                            </systemPropertyVariables>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <artifactId>maven-failsafe-plugin</artifactId>
                <configuration>
                    <skipITs>${skipITs}</skipITs>
                    <trimStackTrace>false</trimStackTrace>
                    <argLine>@{argLine} -Duser.language=el -Duser.country=GR -Duser.timezone=Europe/Athens</argLine>
                    <systemPropertyVariables>
                        <fixtures.dir>${fixtures.dir}</fixtures.dir>
                        <it.db.images>${it.db.images}</it.db.images>
                    </systemPropertyVariables>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco-maven-plugin.version}</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <artifactId>maven-jar-plugin</artifactId>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>${main.class}</mainClass>
                            <addDefaultImplementationEntries>true</addDefaultImplementationEntries>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>

            <plugin>
                <artifactId>maven-shade-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                            <filters>
                                <filter>
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <exclude>module-info.class</exclude>
                                        <exclude>META-INF/versions/*/module-info.class</exclude>
                                        <exclude>META-INF/MANIFEST.MF</exclude>
                                        <exclude>META-INF/*.SF</exclude>
                                        <exclude>META-INF/*.DSA</exclude>
                                        <exclude>META-INF/*.RSA</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ApacheLicenseResourceTransformer"/>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ApacheNoticeResourceTransformer">
                                    <addHeader>false</addHeader>
                                </transformer>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>${main.class}</mainClass>
                                    <manifestEntries>
                                        <Multi-Release>true</Multi-Release>
                                    </manifestEntries>
                                </transformer>
                            </transformers>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
                <version>${spotless-maven-plugin.version}</version>
                <configuration>
                    <java>
                        <includes>
                            <include>src/main/java/**/*.java</include>
                            <include>src/test/java/**/*.java</include>
                        </includes>
                        <palantirJavaFormat>
                            <version>${palantir-java-format.version}</version>
                        </palantirJavaFormat>
                        <removeUnusedImports/>
                    </java>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!-- IDEs built on m2e (Eclipse, VS Code's Java extension) compile with ECJ. m2e sets m2e.version, so the
             IDE gets its own build directory and Maven never tests or packages IDE-compiled classes. -->
        <profile>
            <id>ide</id>
            <activation>
                <property>
                    <name>m2e.version</name>
                </property>
            </activation>
            <build>
                <directory>${project.basedir}/target-ide</directory>
            </build>
        </profile>

        <!-- Testcontainers matrix (needs Docker): ./mvnw verify -Pintegration [-Dit.db.images=mysql:8.4] -->
        <profile>
            <id>integration</id>
            <properties>
                <skipITs>false</skipITs>
            </properties>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.jacoco</groupId>
                        <artifactId>jacoco-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>prepare-agent-integration</id>
                                <goals>
                                    <goal>prepare-agent-integration</goal>
                                </goals>
                            </execution>
                            <execution>
                                <id>report-integration</id>
                                <goals>
                                    <goal>report-integration</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!-- Tier C: the maintainer's gitignored samples/ and test/ databases. Tests skip when they're absent. -->
        <profile>
            <id>local-samples</id>
            <properties>
                <test.excludedGroups>perf</test.excludedGroups>
            </properties>
        </profile>

        <!-- 09: the speed and memory budgets, which are too slow for every build: ./mvnw test -Pperf -->
        <profile>
            <id>perf</id>
            <properties>
                <test.excludedGroups>local-samples</test.excludedGroups>
            </properties>
        </profile>
    </profiles>
</project>
